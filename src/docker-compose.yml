services:
  db:
    image: mysql:8.0
    container_name: Employment
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: InventoryManagementSystem
      MYSQL_USER: user
      MYSQL_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
